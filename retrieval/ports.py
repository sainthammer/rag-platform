"""Абстрактные интерфейсы модуля retrieval.

Отделяет контракты от конкретных реализаций — остальной код
должен зависеть от этих портов, а не от конкретных классов.
"""

from __future__ import annotations

from abc import ABC, abstractmethod

from .pipeline import SourceChunk


class Reranker(ABC):
    """Базовый класс для стратегий повторного ранжирования результатов поиска.

    Принимает список кандидатов из векторного поиска и пересортирует их
    по критерию, более точному или разнообразному, чем исходная похожесть.
    """

    @abstractmethod
    def rerank(self, query: str, candidates: list[SourceChunk]) -> list[SourceChunk]:
        """Пересортировать список кандидатов относительно запроса.

        Args:
            query: Исходный текстовый запрос пользователя.
            candidates: Результаты векторного поиска в порядке убывания score.

        Returns:
            Пересортированный список SourceChunk. Может быть той же длины
            или короче исходного, но никогда длиннее.
        """
        ...
