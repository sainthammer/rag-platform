
# Пометка на будущее для студентов

Данная тема очень объемная и сложная для понимания "с наскока", это нормально если вы что-то не поймете из материала. даже после полной сдачи темы. В парадигму ООП довольно тяжело "въехать" полноценно без достаточной практики. Поэтому привыкните к тому, что вы что-то не поняли до конца, позже вы вернетесь к этим знаниям уже с практикой применения без понимания и теория будет усваиваться лучше. Вы можете периодически возвращаться к непонятным моментам, чтобы лучше их понять.

# **В этой теме вы изучите:**

- Основы парадигмы объектно ориентированного программирования (ООП).
- классы и их создание.
- Основные принципы ООП.
- Научитесь писать код на основе ООП.
- Изучите  принципы SOLID и другие правила написания чистого кода.

---
# **Теория**

## Обязательно

#### **ООП**

1. Что такое ООП? В чём его плюсы и минусы?
2. Принципы ООП.
3. Что такое наследование?
4. Что такое композиция?
5. Что такое агрегирование?
6. Что такое инкапсуляция? примеры
7. Что такое полиморфизм? Приведите пример полиморфизма?

#### **Модификаторы и декораторы**

1. Какие модификаторы доступа могут быть у метода класса?
2. Перечислите все модификаторы доступа в порядке уменьшения закрытости, какую область видимости они имеют?
3. Что такое декораторы?
4. Сравнение classmethod vs staticmethod vs обычный метод без декораторов. Расскажите, как они ведут себя при наследовании, для чего применяются и можно ли их использовать внутри друг друга.
5. Можно ли использовать статические методы в обычных? Наоборот? Почему?
6. Для чего используется декоратор @property?
7. Как используется декоратор @singledispatch из библиотеки functools.

#### **Классы. Методы**

1. Что такое класс?
2. Что такое объект?
3. Что такое переопределение и как её сделать?
4. Что такое перегрузка и как её сделать?
5. Абстрактные классы
6. Что такое абстрактный класс?
7. Как реализовать абстрактный класс в python?

#### **Интерфейсы**

1. Что такое интерфейс?
2. Как реализовать интерфейс в python?

#### **ЛЯМБДА**

1. Лямбда функции
2. Как реализуются лямбда функции в ptyhon?

#### **Общие вопросы по ООП Python**

1. Что такое self и cls в методах класса?
2. Что такое сеттер?
3. Что такое геттер?
4. Что такое дескрипторы данных и не данных?
5. Что такое super() в методах классах?
6. Что такое dunder (магические) методы (знать часто используемые) в Python?
7. Какие магические методы управляют жизненным циклом объекта класса (создание, инициализация и удаление) и как они работают?
8. Как написать свой контекстный менеджер в классе? Какие аргументы у метода `__exit__`?
9. Как сравнивать объекты одного класса через операторы `==,` `>` и т.п.
10. Как работать объекту с математическими операциями?
11. Как сделать класс неизменяемым с точки зрения языка и по факту полностью неизменяемым? Какие магические методы надо переопределить?
12. В чем разница между `__getattr__` и `__getattribute__`?
13. Как посмотреть все методы и атрибуты у объекта класса?
14. Магический атрибут `__slots__` и `__dict__`, в чем разница?
15. Разница между `__copy__` и `__deepcopy__`?
16. В чем разница между `__str__` и `__repr__`?
17. Какие магические методы нужны у класса, чтобы его объект был итерируемым? Что они возвращают?
18. Что такое MRO? Как это работает в Python? Как узнать в каком порядке будут искаться методы при множественном наследовании? Алгоритм линеаризации C3 и проблема ромбовидного наследования (проблема алмаза).

#### **Принципы написания чистого кода в ООП**

1. Что такое SOLID? Понимание всех принципов на примерах в коде.
2. Принцип DRY
3. Принцип KISS
4. Принцип YAGNI

---
# Практика:

### Обязательно

1. Посмотрите первые 21 видео про ООП на python  
    [https://www.youtube.com/watch?v=Z7AY41tE-3U&list=PLA0M1Bcd0w8zPwP7t-FgwONhZOHt9rz9E](https://www.youtube.com/watch?v=Z7AY41tE-3U&list=PLA0M1Bcd0w8zPwP7t-FgwONhZOHt9rz9E "smart-link")
2. Прочитайте статью по магическим методам [https://habr.com/ru/articles/186608/](https://habr.com/ru/articles/186608/ "smart-link")
3. Создайте класс `MeansOfTransport` (для определения цвета и марки машины), принимающий 2 аргумента при инициализации (марка и цвет транспортного средства). В этом классе реализуйте методы `show_color()`, выводящий на печать цвет транспортного средства и `show_brand()`, для получения марки транспортного средства.
4. Работаем с классом из 3 пункта. Реализуйте сеттеры и геттеры для цвета и марки транспортного средства.
5. Реализуйте два класса `Car` и `Moped`, которые будут наследоваться от класса MeansOfTransport. При инициализации они должны принимать новый аргументы(количество колес.
6. В классе `Moped` напишите статическую функцию, которая на вход будет принимать расстояние и максимальную скорость, а на выходе получать время, за которое проедет мопед это расстояние.
7. Попробуйте инициализировать несколько любых переменных в классе `Car` и сделайте одну переменную приватной, одну защищенной. Попробуйте получить к ним доступ. Какой результат?
8. В классе `Car` добавьте переменную класса `car_drive = 4` и реализуйте classmethod, который выводит на экран переменную `car_drive`
9. Реализуйте все возможные магические методы в классе Car. (Все сразу у вас не получится сделать, некоторые магические методы конфликтуют друг с другом, чтобы обойти это сделайте другой класс, разделите методы между ними)
10. Реализуйте класс `Calculator`, в котором будет один метод, для вычисления суммы двух чисел.  
    Реализуйте еще один класс, который будет наследоваться от класса Calculator и перегрузите метод для вычисления суммы двух чисел, чтобы он делал конкатенацию двух строк.
11. Реализуйте абстрактный класс `Animals`, создайте класс Cat, который будет наследоваться от класса Animals и реализуйте метод voice.
12. Прочитайте статью и выполните действия, которые в ней прописаны [https://pythonist.ru/vvedenie-v-mnozhestvennoe-nasledovanie-i-super/](https://pythonist.ru/vvedenie-v-mnozhestvennoe-nasledovanie-i-super/ "smart-link")
13. Реализуйте класс Dog. Придумайте, какие переменные будет принимать данный класс и какие методы будут реализованы. Реализуйте в этом классе паттерн Singleton  
    Пример: [https://pythonpip.ru/osnovy/shablon-proektirovaniya-singleton-v-python](https://pythonpip.ru/osnovy/shablon-proektirovaniya-singleton-v-python "smart-link")
14. Напишите класс, который принимает список людей с интерфейсом добавления новых и при итерации возвращал имена людей
15. Напишите класс, содержит 3 любые атрибута и при изменении логгировать (выводить) всё в консоль (при изменении выводит сообщение в формате "старое -> новое")

### Дополнительно

1. Создайте класс `TextFile`, который будет выступать контекстным менеджеров по работе с файлом sample_file.txt. Попробуйте с помощью объекта этого класса сделать новые записи в файле.
2. Сделайте класс `GenerateFibonachi`, который генерирует числа Фибоначчи.
3. Создайте класс функтуру `Calculator`, который умеет складывать, вычитать, делить и умножать два числа. (Вам нужно будет определить метод `__call__`, который принимает в качестве аргументов оператор ('add', 'sub', 'div' или 'mul') и два числа x1 и x2, например. В результате вызова метода должно вернуться значение в зависимости от указанного оператора. Используйте словарь в качестве альтернативы if - else, а действия напишите либо через lambda функции или используйте статические методы, а лучше сделайте две реализации.
4. Сделайте класс `NonMutable`, который является полностью неизменяемым singleton'ом, у которого атрибуты нельзя больше поменять после инициализации, у которого нет возможности добавить новые атрибуты (вам понадобиться поработать с методами `__new__`, `__init__`, `__slots__` и  `__setattr__`)
5. Теперь добавьте хэшируемость для объекта класса из предыдущего задания. (нужно определить метод `__hash__` и `__eq__`)
6. Напишите сложную систему из классов с многоуровневым множественным наследованием. Исследуйте проблему MRO алмаза (ромбовидное) и алгоритм линеаризации C3 в своем коде. Попробуйте обойти ваш MRO через прямое обращение к нужному методу через super().
7. Напишите код, в котором будет намерено нарушены все принципы SOLID по одному на каждый пример. Потом пример кода с исправлением нарушения. Так вы лучше их поймете на практике, чем 100 раз заучивать слова в определении.

Python_Theory_v_1.0.1