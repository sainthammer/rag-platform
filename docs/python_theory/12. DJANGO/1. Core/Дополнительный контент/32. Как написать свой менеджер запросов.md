
Согласно документации по django, чтобы сделать свой менеджер запросов, нам понадобиться из  `django.db` импортировать `models` там нам нужен класс `Manager`, и унаследовать от него свой кастомный менеджер:

```python

from django.db import models

class ArticleManager(models.Manager):
    """
    Кастомный менеджер для модели статей
    """

    def all(self):
        """
        Список статей (SQL запрос с фильтрацией для страницы списка статей)
        """
        return self.get_queryset().filter(status='published')


myobjects = ArticleManager()

queryset1 = YourModelHere.myobject.all()

```