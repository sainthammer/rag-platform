
# Формулировка

Выведите имя самого старшего человека. Если таких несколько, то выведите их всех.

Поля в результирующей таблице:

member_name

# Моё решение

```sql

SELECT member_name FROM FamilyMembers
WHERE birthday = (
    SELECT MIN(birthday) AS 'min' FROM FamilyMembers
    );

```

