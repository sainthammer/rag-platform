
У нас в прогремме нет и не было отдельного выделяния на этот блок. Однако Docker и Docker compose это необходимый инструмент для уверенного разраба. Поэтому буду выделять какие-то вопросы по нему отдельно сюда.

Вопросы и статьи:

