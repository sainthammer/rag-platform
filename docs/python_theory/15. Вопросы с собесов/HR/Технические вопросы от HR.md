
# Программирование

Отличие "Направление зависимости" от "направления потока данных"

---
# Python

Что такое GIL и зачем он нужен?
Какие ограничения накладывает GIL?
Почему в программах на Python нет проблемы "состояния гонок данных"?
Что такое декораторы? Приведи примеры использования декораторов у тебя в работе.
Разница между наследованием и композицией.
Расскажите про декоратор.
Есть ли множественное наследование в Python?
Какие типы данных являются не изменяемыми?
Какие есть виды генераторов в Python?
Назовите области видимости в Python?
Как вы обрабатываете исключения в Python?
Каким свойством должен обладать объект, чтобы быть ключом в словаре?
Итерируемый объект в Python согласно итерационному протоколу должен реализовать метод `__next__` да или нет?
Является ли строка изменяемой?
Можно ли использовать tuple ключом в словаре? - да
Как обозначается модификатор доступа private в Python? - два нижних подчеркивания
Можно ли восстановить объект, если есть результат выполнения hash функции?
Как в работающей программе на Python создать новый класс с произвольным количеством полей и методов?
Как в Python избегать проблемы "Ромбовидного наследования"?
Как объект в Python заставить работать как функцию?
Как в Python числовом полю класса предотвратить присвоение отрицательного значения?

---
# Параллелизм и конкуренция

Как с помощью асинхронных функций ускорить тригонометрические вычисления?
Как ускорить выполнение программы на Python при сетевых операциях? (тут как раз и имелось ввиду сделать через асинхронку)
Чем отличаются потоки от процессов?
Как организовать обмен данными в параллельных частях одной программы на Python?  (Я уточнил вопрос: В одной части идет прием данных от пользователя. в другой с Redis, в другой части результаты запросы от БДшки, как собрать с них данные?)

---

# Паттерны

Какие виды порождающих паттернов вы знаете?


---

# БД

Что такое индекс в БД и зачем он нужен?
Свойства транзакций в РБД? - тут про ACID шла речь.
Сколько будет строк в CROSS JOIN (левая 5 строк, права таблица 10 строк)? - до 50, декартово перемножение
Сколько будет строк в INNER JOIN (левая 5 строк, права таблица 10 строк)? -  максимум 5, по меньше, может быть 0, если нет совпадений по условию JOIN'а

---

# Интернет и устройство сетей

Почему используют IPv6, если есть IPv4?  
Что такое NAT?

---

# Linux

как посмотреть IP?  
Какой порт для подключения по SSH?  
Как посмотреть что занимает место на диске?


---

# Приложения

Что такое REST API? (надо рассказать про принципы REST тоже)

---
# Django:

Как разделяются поля в Django ORM? - Мне сказали про два _
Как создать и применить миграцию в Django ORM?
Как обозначается Primary Key в Django ORM?
Какой шаблон реализует Django? ( Я сначала не понял вопрос, начал про монолит рассказывать из серии игр СТАЛКЕР. А потом вспомнил про MVT, он же вариация MVC)


---

# Git:

git commit -amend что делает?  
команда fix up в git что делает?