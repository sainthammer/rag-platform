
# Python

как передаются аргументы в Python (например в функцию)
что такое хэшируемый объект
В какие моменты отрабатывает Сборщик мусора Python?
Из чего состоит словарь - речь про хеш таблицу, решение коллизий
что такое декораторы?
Как работает память в Python? 
Что такое интернирование малых целых чисел и строк в Python и зачем оно нужно?
Что такое интерполяцию строк и зачем?
Про itertools

# Асинхронность и мультипоточка / -процессинг 

Расскажите разницу между асинхронным и многопоточным подходом.
Где использовать многопоточность, а где асинхронность
Расскажите про GIL
asyncio.to_thread
Как в асинхронном коде, запустить синхронную задачу - через многопоточку 

# БД

Какие типы индексов бывают - B-tree, hash И Т.Д. (Самые частые на практике это B-Tree, GIN и Hash индексы в PostgreSQL:)
преимущества sql / nosql, 
варианты оптимизации, увеличения производительности 
Что такое Vacuum в SQL
Какая сложность поиска в b-tree - там алгоритм бинарного поиска O(log n)
Как вызвать исключающий запрос в БД

# Разработка приложений

Как под капотом реализуются механизмы авторизации? Какие способы есть (через куки и сессии), как они реализованы, чем отличаются?


# Django

Что такое Q в Django
Что такое F в Django

# Fast API

Что такое Depends - инъекция зависимостей
CORS что это и зачем?
Что такое BackgroundTask? Как использовать? Какие есть аналоги?

# Rabbit MQ

Как реализовать идемпотентность в RabbitMQ

# Тестирование

Про parametrize в pytest (конкретно про то, как элегантно заставить тест скипаться, не закомментив его)


# Архитектура

Расскажи про архитектуру своего проекта (рассказать как общались микросервисы, какие паттерны, какие библиотеки были использованы)
Расскажите преимущества и недостатки монолита и микросервисов в сравнении.
паттерн сага, 
мб проектирование сервиса, 
trace_id и transaction_id, очереди - какое-нибудь различие fan-in и fan-out

# Docker

docker compose: какая минимальная реализация сервисов нужна например для какого-либо приложения с бд и фронтендом, как настроить взаимодействие между ними